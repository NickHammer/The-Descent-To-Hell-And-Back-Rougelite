/**
 * Preload script. The game needs no native APIs yet, so nothing is exposed —
 * this is the seam where a contextBridge API goes when desktop features
 * (file-based saves, rich presence) are added.
 */
export {};
